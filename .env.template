# =============================================================================
# NBA AI Environment Configuration
# =============================================================================
# Copy this file to .env and update with your values
# DO NOT commit .env to git (it's in .gitignore)

# -----------------------------------------------------------------------------
# Project Paths (REQUIRED)
# -----------------------------------------------------------------------------
# Absolute path to the project root directory
PROJECT_ROOT=/path/to/your/project/root

# Path to the active SQLite database (relative to PROJECT_ROOT or absolute)
# Options:
#   - data/NBA_AI_2023_2025.sqlite (working DB, 2.1GB, last 2-3 seasons)
#   - data/NBA_AI_ALL_SEASONS.sqlite (master archive, 24GB, all seasons)
DATABASE_PATH=data/NBA_AI_2023_2025.sqlite

# -----------------------------------------------------------------------------
# Web App Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# Flask secret key for session security
# If not set, Flask will auto-generate one (not persistent across restarts)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# WEB_APP_SECRET_KEY=your_generated_secret_key_here

# -----------------------------------------------------------------------------
# API Keys (OPTIONAL - Only needed for specific predictors)
# -----------------------------------------------------------------------------
# OpenAI API key - removed Nov 2025
# GPT4Mini predictor removed, rebuilding GenAI from scratch

# -----------------------------------------------------------------------------
# Airflow Configuration (OPTIONAL - Only if using Airflow DAGs)
# -----------------------------------------------------------------------------
# AIRFLOW_HOME=${PROJECT_ROOT}/airflow
# AIRFLOW__CORE__FERNET_KEY=generate_with_python_cryptography_fernet