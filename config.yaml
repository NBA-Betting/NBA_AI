project:
  root: "${PROJECT_ROOT}"  # Should be updated in the .env file.

database:
  path: "${DATABASE_PATH}"  # Set via DATABASE_PATH in .env file

# Limited to seasons with data in database.
# Developers can add more seasons locally if needed.
# Database Updater supports all seasons back to 2000-2001.
api:
  max_game_ids: 20 # Only applies to game_ids input of Games API. Not the date input.
  # Older seasons to keep serving. The current season and the one before it are
  # always accepted on top of this list, so the API survives a season rollover
  # without an edit here. See get_valid_seasons() in src/games_api/api.py.
  valid_seasons:
    - "2023-2024"
    - "2024-2025"
    - "2025-2026"

web_app:
  secret_key: ${WEB_APP_SECRET_KEY} # Should be set in the .env file. Will be set automatically if not set in .env

nba_api:
  pbp_live_endpoint: "https://cdn.nba.com/static/json/liveData/playbyplay/playbyplay_{}.json"
  pbp_stats_endpoint: "https://stats.nba.com/stats/playbyplayv3?GameID={}&StartPeriod=0&EndPeriod=0"
  schedule_endpoint: "https://stats.nba.com/stats/scheduleleaguev2?Season={season}&LeagueID=00"
  players_endpoint: "https://stats.nba.com/stats/commonallplayers?LeagueID=00&Season={season}"
  pbp_live_headers:
    Accept: "text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9"
    Accept-Encoding: "gzip, deflate, br"
    Accept-Language: "en-US,en;q=0.9"
    Cache-Control: "max-age=0"
    Connection: "keep-alive"
    Host: "cdn.nba.com"
    User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/87.0.4280.88 Safari/537.36"
  pbp_stats_headers:
    Host: "stats.nba.com"
    User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:72.0) Gecko/20100101 Firefox/72.0"
    Accept: "application/json, text/plain, */*"
    Accept-Language: "en-US,en;q=0.5"
    Accept-Encoding: "gzip, deflate, br"
    x-nba-stats-origin: "stats"
    x-nba-stats-token: "true"
    Connection: "keep-alive"
    Referer: "https://stats.nba.com/"
    Pragma: "no-cache"
    Cache-Control: "no-cache"

  schedule_headers:
    User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.3"
    Referer: "https://stats.nba.com/schedule/"
    Accept: "application/json"
    Accept-Language: "en-US,en;q=0.9"
    Cache-Control: "no-cache"

# Set Default Predictor
default_predictor: "Tree"

# Predictors Configuration
# Models v0.5: trained on all seasons through 2024-2025, validated on 2025-2026
# Ridge MAE 9.7, XGBoost MAE 9.6, MLP MAE 9.5 (retrained Apr 2026)
predictors:
  Baseline: {}  # Formula-based, no models required
  Linear:
    model_paths:
      - "models/linear/model.joblib"
  Tree:
    model_paths:
      - "models/tree/model.joblib"
  MLP:
    model_paths:
      - "models/mlp/model.pth"
  # Ensemble:  # Requires PyTorch for MLP component
  #   model_paths:
  #     Linear:
  #       - "models/linear/model.joblib"
  #     Tree:
  #       - "models/tree/model.joblib"
  #     MLP:
  #       - "models/mlp/model.pth"
  Phase5:  # Phase 5 Hierarchical (L1→L2→L3→L4)
    model_paths:
      l2: "models/phase5/l2.pt"
      l3l4: "models/phase5/l3l4.pt"
  Phase3:  # Phase 3 Exp 5 Transformer (roster-temporal)
    model_paths:
      checkpoint: "models/phase3/model.pt"
      config: "models/phase3/config.yaml"
  Ensemble:  # Combines Phase5, Phase3, Linear, Tree, MLP predictions
    model_paths: {}